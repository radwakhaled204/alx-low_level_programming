#include "main.h"

/*
 * add - function to add of two integers
 *
 * @x: first input.
 * @y: second input.
 *
 * Return: result of x + y
 */

int add(int x, int y)
{
	int x = 0;
	int y = 0;
	int result = x + y;

	return (result);
}
